
document.querySelector("#auth-login").addEventListener("click", doLogin);

async function doLogin(){
    let PassportUserData={};
    PassportUserData.email=document.querySelector("#nigger input[name=email]").value;
    PassportUserData.password=document.querySelector("#nigger input[name=password]").value;
    alert(PassportUserData.email);
    alert(PassportUserData.password);
}